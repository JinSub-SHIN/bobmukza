# 모바일 앱 빌드 가이드 (한글)

## ⚠️ 중요 사항

**아이폰에서는 APK를 실행할 수 없습니다!**
- **APK**: Android 기기 전용 (갤럭시, 안드로이드 폰)
- **IPA**: iPhone(iOS) 전용
- 아이폰에서 앱을 실행하려면 **IPA 파일**이 필요합니다.

## 현재 환경 (Windows)

Windows에서는 **Android APK만 빌드 가능**합니다.
- ✅ Android APK 빌드 가능
- ❌ iOS IPA 빌드 불가능 (macOS 필요)

## Android APK 빌드 방법

### 1단계: 프로젝트 빌드

```bash
npm run build
```

### 2단계: Android 플랫폼 추가

```bash
npx cap add android
```

### 3단계: 프로젝트 동기화

```bash
npx cap sync
```

### 4단계: Android Studio 열기

```bash
npx cap open android
```

### 5단계: Android Studio에서 APK 빌드

1. Android Studio가 열리면 잠시 기다립니다 (Gradle 동기화)
2. 상단 메뉴: `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`
3. 빌드 완료 후 하단에 알림이 표시됩니다
4. "locate" 링크를 클릭하거나 다음 경로로 이동:
   ```
   android/app/build/outputs/apk/debug/app-debug.apk
   ```

### 6단계: APK 설치

1. APK 파일을 Android 기기로 전송 (USB, 이메일, 클라우드 등)
2. Android 기기에서:
   - 설정 → 보안 → "알 수 없는 출처" 설치 허용
   - APK 파일을 클릭하여 설치

## iOS IPA 빌드 (아이폰용)

**Windows에서는 불가능합니다!** macOS가 필요합니다.

### 필요한 것:
1. **macOS** (Mac 컴퓨터)
2. **Xcode** (Mac App Store에서 다운로드)
3. **Apple Developer 계정** (실제 기기에 설치하려면)

### macOS에서 빌드하는 방법:

```bash
# 1. 프로젝트 빌드
npm run build

# 2. iOS 플랫폼 추가
npx cap add ios

# 3. 동기화
npx cap sync

# 4. Xcode 열기
npx cap open ios

# 5. Xcode에서 Archive → Distribute App
```

## 빠른 시작 (Android APK만)

```bash
# 1. 빌드
npm run build

# 2. Android 추가
npx cap add android

# 3. 동기화
npx cap sync

# 4. Android Studio 열기
npx cap open android
```

그 다음 Android Studio에서 APK를 빌드하세요!

## 문제 해결

### Node.js 버전 경고
- 현재 Node.js 14를 사용 중이지만, Capacitor는 동작합니다
- 가능하면 Node.js 18 이상으로 업그레이드 권장

### Android Studio가 없을 때
1. [Android Studio 다운로드](https://developer.android.com/studio)
2. 설치 후 Android SDK 설치
3. Java JDK 17 이상 설치 필요

### 빌드 오류 발생 시
- Android Studio에서 `File` → `Sync Project with Gradle Files` 실행
- `Build` → `Clean Project` 실행 후 다시 빌드

## 다음 단계

APK를 빌드한 후:
1. Android 기기에 설치하여 테스트
2. 문제가 있으면 Android Studio의 Logcat에서 오류 확인
3. 앱 아이콘, 이름 등은 `android/app/src/main/res/` 폴더에서 수정

## 참고

- 자세한 내용은 `MOBILE_APP_GUIDE.md` 파일 참고
- Capacitor 공식 문서: https://capacitorjs.com/docs

